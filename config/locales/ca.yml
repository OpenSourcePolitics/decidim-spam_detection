---
ca:
  decidim:
    components:
      spam_detection:
        name: SpamDetection
    spam_detection:
      spam_detection_mailer:
        notify_detection:
          blocked_count: blocked_count %{count}
          hello: Hello %{name}
          intro: ''
          reported_count: reported_count %{count}
    spam_detection_mailer:
      notify_detection:
        subject: Subject
